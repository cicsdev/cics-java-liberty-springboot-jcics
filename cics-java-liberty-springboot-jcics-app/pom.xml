<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  
  <!-- Inherit from parent POM -->
  <parent>
    <groupId>com.ibm.cicsdev</groupId>
    <artifactId>cics-java-liberty-springboot-jcics</artifactId>
    <version>1.0.0</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  
  <!--  Application module properties -->
  <artifactId>cics-java-liberty-springboot-jcics-app</artifactId>
  <name>com.ibm.cicsdev.springboot.jcics.app</name>
  <description>Demo project for Spring Boot JCICS with CICS Liberty - Application</description>
  
  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot.repackage.skip>true</spring-boot.repackage.skip>
  </properties>

  <!-- CICS TS V6.1 BOM (as of Sept 2024) -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.ibm.cics</groupId>
        <artifactId>com.ibm.cics.ts.bom</artifactId>
        <version>${cics.bom.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  
  <dependencies>
    <!-- Spring Boot web support -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Exclude only the Tomcat embedded container jars from the WAR — Liberty provides the servlet container.
         In Spring Boot 4.x, spring-boot-starter-tomcat pulls in spring-boot-starter (and thus all core
         Spring libraries) as a compile dependency, so using provided scope on the full starter would
         incorrectly exclude spring-core, spring-context etc. from WEB-INF/lib. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-tomcat-runtime</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Compile against, but don't include JCICS in the final build (version and scope are from BOM) -->
    <dependency>
      <groupId>com.ibm.cics</groupId>
      <artifactId>com.ibm.cics.server</artifactId>
    </dependency>
  </dependencies>

  <!-- Package as an executable war (default jar) -->
  <packaging>war</packaging>
  <build>
    <finalName>cics-java-liberty-springboot-jcics</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
